PMS
Personal Management System

项目管理工具
Maven
Git	https://github.com/i2019/pms.git

JDK
1.7

框架
myBatis
Spring
SpringMVC

数据库
MySQL

前端框架
AngularJS

需求
1.
增加记录日常消费的功能










SQL:Create DB and Table

CREATE DATABASE IF NOT EXISTS `pms` DEFAULT CHARACTER SET utf8;
USE `pms`;

CREATE TABLE `consumption` (
  `id` VARCHAR(32) NOT NULL COMMENT '支出id',
  `amount` decimal(19,4) NOT NULL default '0.0000' COMMENT '支出金额',
  `cause` VARCHAR(256) DEFAULT NULL COMMENT '支出原因',
  `consumer` VARCHAR(32) DEFAULT NULL COMMENT '支出人',
  `time` DATETIME DEFAULT NULL COMMENT '支出时间',
  `createTime` DATETIME DEFAULT NULL COMMENT '创建时间',
  `modifyTime` DATETIME DEFAULT NULL COMMENT '修改时间',
  `createdBy` VARCHAR(32) DEFAULT NULL COMMENT '创建者',
  `updatedBy` VARCHAR(32) DEFAULT NULL COMMENT '修改者',
  `remark` VARCHAR(1024) DEFAULT NULL COMMENT '备注',
  `delFlag` TINYINT(1) DEFAULT NULL COMMENT '删除标志',
  PRIMARY KEY  (`id`)
) ENGINE=INNODB DEFAULT CHARSET=utf8;


















